!(function (a) {
    "use strict";
    a("body").on("click", "#checkCoupon1", function (t) {
        t.preventDefault();
        var i = a(this),
            o = a("#coupon_input"),
            pr = a("#web_id1"),
            s = o.parent().find(".invalid-feedback"),
            n = o.val(),
            my = pr.val();
            
        o.removeClass("is-invalid is-valid"),
            s.text(couponInvalidLng),
            n
                ? (i.addClass("loadingbar primary").prop("disabled", !0),
                  a
                      .post("/cart/coupon/validate1", { coupon: n ,web_id1: my}, function (t) {
                          t && 200 == t.status
                              ? ( o.addClass("is-valid"))
                              : t && 422 == t.status && (o.removeClass("is-valid"), o.addClass("is-invalid"), s.text(t.msg));
                      })
                      .always(function () {
                          i.removeClass("loadingbar primary").prop("disabled", !1);
                      }))
                : o.addClass("is-invalid");
    });
})(jQuery);
